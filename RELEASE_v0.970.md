# GTA 산안드레아스 · PS2 한글 패치 v0.970

**Grand Theft Auto: San Andreas (PlayStation 2, NTSC-J) 한글 패치입니다.**

> 자료 유실이 우려되어 이번 **v0.970**부터 GitHub에 등록을 시작합니다.
> 이전 판본(v0.900 · v0.969)은 완성도 낮은 **로스트 미디어**로 취급해 주세요. 😄

워리어스 한글패치 후속작. 일본판을 기반으로 대사·메뉴·자막을 한글화하고, 폰트를 전면 교체했으며, 북미판 수위의 검열 해제를 포함합니다.

---

## v0.970에서 달라진 것

- **폰트 전면 교체 — 사천항공체(자체 이탤릭).** 한글·영문·숫자·기호를 하나의 서체로 통일했습니다. 한글 옆에서 겉돌던 원판 라틴 폰트를 대체하고, 이전 폰트에서 글자 사이에 점이 튀거나 획이 겹치던 오염을 전부 제거했습니다. 게임 실제 표시 좌표(ELF 직접 실행으로 측정)에 맞춰 셀 안에 정렬했습니다.
- **자막 줄바꿈 교정.** 어절이 중간에서 뚝 끊기던 것을, 게임의 컷신(24자)·필드 대사(19자) 글자수 한도에 맞춰 어절 단위로 다시 정렬했습니다. 짧은 꼬리 줄이 생기지 않도록 균형 분할을 씁니다.
- **상점·메뉴 프롬프트 통일.** 의상점·이발소·타투·색상·댄스 등에서 3줄로 넘쳐 메뉴 박스와 겹치던 안내문을, 원판 형식(아이콘+라벨 2줄)과 통일된 어휘(결정/입어보기/구입/뒤로/그만두기…)로 정리했습니다.
- **라디오 방송국명 원판 복귀.** Radio Los Santos, K-Jah West, Bounce FM, Playback FM 등 방송국명을 원판 라틴 표기로 되돌렸습니다.
- **은신처 표시 원판 복귀.** 진입 시 뜨던 "은신처"를 원판대로 `Safe House`로 되돌렸습니다.
- **체육관 안내문 교정.** 오역·잘못된 조작 안내를 원문("~x~와 ~o~를 번갈아 연타")대로 고쳤습니다.
- **이전 세이브 정화 도구 첨부** (아래 참조).

v0.900/v0.969의 기존 성과(검열 해제, 미션명 오버플로 교정, 요일 표시, 차량명 영문 고정 등)는 그대로 포함됩니다.

---

## 적용할 원본

이 패치는 **NTSC-J 일본판 ISO 하나에만** 적용됩니다. ROM은 배포하지 않습니다.

| | |
| --- | --- |
| 파일명 | `Grand Theft Auto San Andreas (NTSC-J).iso` |
| 크기 | 4,495,507,456 바이트 |
| **MD5** | `aa7cdf78c953f898443dbb5f89c3187f` |
| SHA-1 | `5dfb598053c4dbc1c87c407fc70fbeef6582bb76` |

---

## 적용 방법

**1단계 — 한글 패치 (필수)**

```
xdelta3 -d -s "Grand Theft Auto San Andreas (NTSC-J).iso" GTA_SA_KR_v0.970_FULL.xdelta "GTA SA 한글 v0.970.iso"
```

**2단계 — 핫커피(성인 콘텐츠) 복원 (선택)**

1단계 결과 ISO에 이어서 적용합니다.

```
xdelta3 -d -s "GTA SA 한글 v0.970.iso" GTA_SA_KR_v0.970_hotcoffee.xdelta "GTA SA 한글 v0.970 핫커피.iso"
```

GUI가 편하면 [DeltaPatcher](https://github.com/marco-calautti/DeltaPatcher), 명령줄은 [xdelta3 공식 배포판](https://github.com/jmacd/xdelta-gpl/releases)을 쓰세요. (Source에 기준 ISO, Patch에 xdelta 파일)

### 적용 후 확인

**1단계 결과 (한글판 · 기본)**

| | |
| --- | --- |
| 크기 | 4,495,507,456 바이트 |
| **MD5** | `1c6b2de0223e6185320e1f0a4d780c45` |
| SHA-1 | `1090fecdad612ef42c91e8e0db14bb6b700f5df6` |
| CRC32 | `DDABE625` |

**2단계 결과 (핫커피 적용판)**

| | |
| --- | --- |
| 크기 | 4,495,507,456 바이트 |
| **MD5** | `2a98cad29c15eb3697f00a63f89e190f` |
| SHA-1 | `55606ba73cc0935089e8315223b8f1fe290c2692` |
| CRC32 | `2DF389BA` |

---

## 이전 세이브 이어 쓰기

검열판이나 이전 버전에서 만든 세이브를 이 패치로 이어 쓰면 **의상점 데이트가 진입 시 멈추는** 문제가 있습니다(세이브에 박힌 플래그가 원인). 이럴 때만 아래 도구로 세이브를 한 번 고쳐 주세요. **새로 시작한 게임에는 필요 없습니다.**

- `gtasa_save_fix.html` — 브라우저에서 열어 `Mcd001.ps2`를 끌어다 놓으면 고친 파일을 내려받습니다. 설치 불필요, 파일은 PC 밖으로 나가지 않습니다.
- `gtasa_save_fix.py` — 명령줄: `python gtasa_save_fix.py Mcd001.ps2` (원본은 `.bak` 자동 백업)

PCSX2 메모리카드(`Mcd001.ps2`)와 폴더식 메모리카드의 세이브 파일(`*.b`) 모두 지원합니다. 세이브의 데이트 플래그(오프셋 `0x14D6`)를 켜고 체크섬·ECC를 다시 계산합니다.

---

## 패치 파일

**`GTA_SA_KR_v0.970_FULL.xdelta`** — 원본 → 한글판(기본)

| | |
| --- | --- |
| 크기 | 528,750 바이트 |
| MD5 | `24efb8d884fa0b76e69e51f515a62229` |
| SHA-1 | `8f194a9c8bdc6422fcd811456dfd9acef10d55dc` |
| CRC32 | `D39FFCC1` |

**`GTA_SA_KR_v0.970_hotcoffee.xdelta`** — 한글판 → 핫커피 적용판

| | |
| --- | --- |
| 크기 | 185,137 바이트 |
| MD5 | `96e2e6926244e014a994eb8a6d59f147` |
| SHA-1 | `46982e0471c27e1f0f2516eaa8aedb5fb27f4517` |
| CRC32 | `D9C1105E` |

두 패치 모두 만든 뒤 원본에 다시 적용해 결과가 빌드본과 **바이트 단위로 동일한 것**을 확인했습니다 (기본판 CRC `DDABE625`, 핫커피판 CRC `2DF389BA`).

---

## 곁들이면 좋은 것 (PCSX2 치트)

`.pnach`를 `PCSX2\cheats\`에 넣고 게임 속성 → Cheats → Enable Cheats 를 켠 뒤 목록에서 고르세요. 한글 패치가 CRC를 원본과 동일하게 맞춰놨기 때문에 그대로 먹습니다.

- **화면 개선** `SLPM-65984_60FE139C_와이드픽스.pnach` — 와이드스크린 16:9, 레이더 원형 보정, 고스팅 제거, 60 FPS, 항공기 계기 정렬
- **조작 편의** `SLPM-65984_60FE139C_치트.pnach` — 무적, 무한 체력/아머/탄약/스프린트, 수배 0, 돈, 근육/스태미너 등 17종

---

## 솔직히 말하면

- 지명·미션명·차량명, 일시정지 메뉴 탭(System, Controls 등)은 영문 원문입니다. 한글로 바꾸면 빅폰트가 안 붙거나 세이브의 미션명이 깨지고, 메뉴는 게임이 잡아둔 폭에 겹칩니다.
- 검열 해제는 북미판 기준으로 되살린 것입니다. 원판보다 수위가 높으니 감안하세요.
- 실기(PS2 본체)에서는 검증하지 않았습니다. **PCSX2 기준**입니다.

---

## 버그 제보

[Issues](../../issues)에 **화면 캡처와 함께** 부탁드립니다. 특히 문장이 중간에서 끊기는 곳, 글자가 네모/이상한 기호로 나오는 곳, 메뉴를 눌렀는데 아무것도 안 뜨는 곳. 번역이 어색한 곳도 환영입니다.

*— GROVE STREET 4 LIFE —*
